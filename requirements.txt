# Update requirements.txt

setuptools
pandas==2.0.3
numpy==1.25.2
requests==2.31.0
scikit-learn==1.3.0
tensorflow==2.12.0" > requirements.txt

# Commit and push changes
git add requirements.txt
git commit -m "Update requirements.txt for compatibility"
git push origin main

# Restart the app
